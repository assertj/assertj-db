create table `group` (
    `read` IDENTITY,
    `by` INTEGER,
    `select` MEDIUMINT,
    `from` INT4,
    `where` VARCHAR,
    `order` NVARCHAR
);

create table `Two Words` (
    `Primary Key` IDENTITY,
    `Column Name` VARCHAR,
    `Test%Test` VARCHAR
);